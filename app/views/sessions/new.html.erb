<div class="container py-4 ">
  <div class="py-2 user-form mx-auto  border-2 bg-white shadow-md">
    <h1 class="text-2xl sm:text-3xl text-center py-4 font-bold">ログイン</h1>
    <div class="form-wide mx-auto">
      <%= form_with(url: login_path, scope: :session, local: true) do |f| %>
      
        <%= f.label :email, "メールアドレス", {class: "text-lg font-bold"} %>
        <%= f.email_field :email, class: 'form-control mb-4' %>

        <%= f.label :password, "パスワード", {class: "text-lg font-bold"} %>
        <%= f.password_field :password, class: 'form-control mb-4' %>

        <%= f.label :remember_me, class: "checkbox inline mb-3" do %>
          <%= f.check_box :remember_me %>
          <span>ログイン状態を維持する場合はチェックをしてください。</span>
        <% end %>
        <div class="text-center my-4">
          <%= f.submit "ログイン", class: "inline-block text-gray-100 bg-green-500 border border-yellow-500 hover:text-green-500 hover:bg-white font-base px-4 py-2 rounded text-base" %>
        </div>
      <% end %>
    </div>
  </div>
</div>