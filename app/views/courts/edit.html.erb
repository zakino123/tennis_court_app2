<% provide(:button_text, 'コート情報変更') %>
<h1>コート情報変更</h1>
<div class="mb-3">
  <%= render 'form' %>
  <%= link_to "コート情報削除", @court, method: :delete,
                                data: { confirm: "削除してよろしいですか?" } %>
</div>